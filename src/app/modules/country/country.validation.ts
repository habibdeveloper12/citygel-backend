// import { z } from 'zod';

// export const SellerZodSchema = z.object({
//   id: z.string().min(1), // Assuming a minimum length of 1 for the ID
//   fullName: z.string().min(1),

//   email: z.string().email(),
//   phoneNumber: z.string(),
//   role: z.string().optional(), // Assuming the role is optional
//   terms: z.boolean().optional(), // Assuming these fields are optional
//   firstVerify: z.boolean().optional(),
//   marketing: z.boolean().optional(),
//   profileImage: z.string().optional(),
// });
